<div class="app-container">
    {{> nav}}
    {{> sidebar}}
    <main class="main-content">
        <h1 style="color: #fff; margin-bottom: 32px;">Liked Videos</h1>
        <div class="video-grid">
            {{#each likedVideos}}
            <article class="video-card">
                <!-- Same video card structure as homepage -->
                <!-- add more asp -->
            </article>
            {{/each}}
        </div>
    </main>
</div>
